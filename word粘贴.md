### word粘贴是怎么实现的
大模型返回的内容是md格式的。我们在渲染大模型返回的消息的时候，使用的是react-markdown库，可以把md的格式转变为html格式。又因为word文档是支持解析html格式的内容的。所以我们在渲染文档的容器上加入一个钩子，等文档渲染完毕就可以使用钩子，获取innerHtml，将其中的html格式的内容复制到系统的剪切板上，这样的内容复制到word以后，word就可以完美的解析其中的内容，并且保证排版不会混乱