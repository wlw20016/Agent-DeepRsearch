### 介绍组件化设计
对于单页面应用来说，组件化开发可以使页面的结构变得更清晰并且增强了可复用性。在这个项目中，核心布局放在App组件中。按照功能将其拆分为左边的历史会话栏，中间的消息渲染栏，右侧的时间线栏。在中间的消息渲染栏中，使用了基于策略模式的消息渲染，做了多类型适配：根据后端返回的 message.type，动态路由到不同的消息渲染组件。比如 TextMessage 处理 Markdown，ToolCallMessage 处理工具调用过程